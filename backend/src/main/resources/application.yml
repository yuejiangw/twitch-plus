spring:
  application:
    name: backend

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/twitch?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: 123456  # update it to your own password
#  sql:
#    init:
#      mode: always
#      schema-locations: "sql/database-init.sql"

  cache:
    caffeine:
      spec: expireAfterWrite=1m

  jackson:
    default-property-inclusion: non_null

  security:
    oauth2:
      client:
        registration:
          twitch:
            authorization-grant-type: client_credentials
            client-authentication-method: client_secret_post
            client-id: "ziejjspwrws09g1bvj3a20kd6frtrf"
            client-secret: "wjot2b6tykirdnuoqwmr2z0oo5sbga"
        provider:
          twitch:
            token-uri: https://id.twitch.tv/oauth2/token

  cloud:
    openfeign:
      oauth2:
        clientRegistrationId: twitch
        enabled: true
      client:
        config:
          twitch-api:
            url: https://api.twitch.tv/helix
            logger-level: basic
            default-request-headers:
              Client-Id: "ziejjspwrws09g1bvj3a20kd6frtrf"

logging:
  level:
    org.apache.coyote.http11.Http11InputBuffer: DEBUG # incoming HTTP requests
    com.twitchplus.backend.external.TwitchApiClient: DEBUG
    org.springframework.jdbc.core: DEBUG  # SQL
